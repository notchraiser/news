import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  box: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
  },
  footer: {
    color: "#fff",
    background: "#000",
    zIndex: 10,
  },
}));
